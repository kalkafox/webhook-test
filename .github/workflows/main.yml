
name: ci-webhook-test

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    container:
      image: archlinux
    steps:
      - run: pacman -Syyu --noconfirm base-devel python3 openjdk8-jdk gradle nano neofetch nginx
      - run: python3 -VVVVVVV
      - run: curl -v https://kalka.io

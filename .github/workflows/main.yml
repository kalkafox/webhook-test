
name: ci-webhook-test

on:
  push:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    container:
      image: archlinux:base-devel
    steps:
      - run: pacman -Syyu neofetch
      - run: neofetch
      - run: curl -v https://kalka.io
